# src/projection/__tests__/websocket-server.test.ts · [[projection-system]]

- waitForMessage · function · L11-L15 — Helper function that waits for a single WebSocket message and parses it as JSON for use in test assertions.
